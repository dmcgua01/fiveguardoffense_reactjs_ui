#TODO: finish writing this file
#install dependencies for makefile
#mac osx
if [[ -d /Users ]]; then
  #TODO: check if thoughtbot developer laptop exists. if not, install it
  #TODO: check if dotfiles exists. if not, install it
  #TODO: brew install any necessary packages
  echo mac
fi

#centos
if [[ -a /etc/redhat-release ]]; then
  #TODO: yum install any necessary packages
  echo centos
fi
